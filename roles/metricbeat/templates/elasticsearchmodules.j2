
- module: elasticsearch
  metricsets:
    - node
    - node_stats
    - index
    - index_recovery
    - index_summary
    - shard
  period: 10s
  hosts: 
{% for host in master_hosts %}
  - "https://{{ host }}:9200"
{% endfor %}

  username: "{{ es_username }}"
  password: "{{ es_password }}"
  ssl.certificate_authorities: ["/etc/metricbeat/certs/elastic-certificates.p12"]
